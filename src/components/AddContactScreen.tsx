import { DataInput } from "./DataInput";

export const AddContactScreen = (props: any) => {
    return (
        <>
            <DataInput navigation={props.navigation}
            />
        </>
    );
}